# GhostTrace — Python Dependencies

# LLM Backend
openai>=1.30.0
httpx>=0.27.0

# CLI & UX
click>=8.1.7
rich>=13.7.0
streamlit>=1.35.0

# Configuration
python-dotenv>=1.0.1
pyyaml>=6.0.1

# Templating
jinja2>=3.1.4

# PDF Export (mandatory)
weasyprint>=61.2
reportlab>=4.2.0

# Charts
plotly>=5.0

# Data handling (used by Streamlit dataframe display)
pandas>=2.0

# Dynamic analysis — live system scan + file detonation
psutil>=5.9.0   # live process/network/DLL enumeration
# winreg is built-in on Windows (no install needed)

# Type checking
mypy>=1.10.0
types-PyYAML>=6.0.12

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Linting / Formatting
ruff>=0.4.4
